# 测试文档 - 侧边栏位置

这是一个测试文档，用于验证：
1. **侧边栏关闭时** - 正文卡片应该居中显示
2. **侧边栏打开时** - 正文卡片应该靠左，减少与大纲的距离

## 测试步骤

1. 打开此文件
2. 按 `Ctrl+Shift+1` 或菜单 `视图 > 大纲` 打开侧边栏
3. 观察正文卡片是否靠左对齐
4. 再次按 `Ctrl+Shift+1` 关闭侧边栏
5. 观察正文卡片是否居中

## 标题二

一些正文内容，用于测试布局效果。

### 标题三

更多的正文内容。

- 列表项 1
- 列表项 2
- 列表项 3

```python
# 代码块测试
def hello():
    print("Hello, Claude Theme!")
```

> 这是一个引用块，测试样式效果

正常的段落文本。
